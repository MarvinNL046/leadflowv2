import {createFileRoute,Link} from '@tanstack/react-router'
import guide from '../../docs/leadgen/bedrijfshandleiding-koppelingen.md?raw'
export const Route=createFileRoute('/crm/settings_/handleiding')({component:Page})
function Page(){return <article className="mx-auto max-w-3xl space-y-4 pb-10"><Link to="/crm/settings">← Instellingen</Link><nav className="flex flex-wrap gap-4 text-violet-700 underline"><Link to="/crm/settings/email">E-mail instellen</Link><Link to="/crm/settings/eigen-whatsapp">WhatsApp instellen</Link><Link to="/crm/settings/sms">SMS instellen</Link><a href="https://resend.com/domains" target="_blank" rel="noreferrer">Resend openen</a><a href="https://wa.voidfix.com/" target="_blank" rel="noreferrer">Voidfix openen</a></nav>{guide.split(/\r?\n\r?\n/).map((block,i)=>block.startsWith('# ')?<h1 key={i} className="text-2xl font-bold">{block.slice(2)}</h1>:block.startsWith('## ')?<h2 key={i} className="pt-5 text-xl font-semibold">{block.slice(3)}</h2>:<p key={i} className="leading-relaxed text-zinc-700">{block}</p>)}</article>}
